Drop the two avatar photos here as PNG files:

    mickey.png
    amanda.png

Recommended: square images, roughly 256x256 px. They are clipped into a
circle for the paddles and the scoreboard, so anything square works.

Until these files exist, the game shows a coloured disc with the
character's initial instead — nothing breaks.
